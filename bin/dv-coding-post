#!/bin/bash
source $(dv-path lib/common.sh) # -*-mode: sh-mode -*-

if [ -f explanation ]; then
    echo "$(tput setaf 2)"
    echo '================================================================'
    cat explanation
    echo '================================================================'
    echo "$(tput sgr0)"
    rm explanation
fi
