#!/bin/bash
# -*-mode: sh-mode -*-
set -euo pipefail

if [ -f explanation ]; then
    echo "$(tput setaf 2)"
    echo '================================================================'
    cat explanation
    echo '================================================================'
    echo "$(tput sgr0)"
    rm explanation
fi

